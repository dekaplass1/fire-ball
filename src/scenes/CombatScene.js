import { SCENES } from '../constants.js';

export default class CombatScene extends Phaser.Scene {
  constructor() {
    super({ key: SCENES.COMBAT });
  }

  create() {
    // Story 2.2 : UI et déroulement visuel d'un combat
  }
}
