// CombatEngine — logique pure de résolution des tours
// AUCUN import Phaser autorisé dans ce fichier
// Implémentation complète à Story 2.1

const CombatEngine = {
  resolveTurn(action) {
    // Story 2.1 : résolution synchrone du tour
    // Retourne { heroHp, enemyHp, log, outcome }
    return null;
  },

  initCombat(level) {
    // Story 2.1 : sélection aléatoire d'un ennemi pour le niveau donné
    return null;
  },
};

export default CombatEngine;
